# MongoDB Connection URI (includes database name)
spring.data.mongodb.uri=mongodb://localhost:27017/expense-splitter

# Optional: Enable detailed MongoDB operations logging (for debugging)
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
